{
  "name": "test-kitchen-site",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "postbuild": "pagefind --site dist",
    "astro": "astro",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "validate": "node scripts/validate-content.js",
    "lint:recipes": "node scripts/lint-recipes.js"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/mdx": "^4.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "gray-matter": "^4.0.3",
    "pagefind": "^1.3.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
